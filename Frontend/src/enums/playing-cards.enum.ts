export enum PlayingCardType {
  BEKANNT = 1,
  GEFRAGT = 2,
  ANGESETZT = 3,
  GESCHUETTELT = 4,
  ORIGINAL = 5,
  JOKER = 6
}

export enum PlayingCardState {
  OPEN = 'shown',
  HIDDEN = 'hidden'
}

export enum DeckState {
  PULLED = 'pulled',
  NOT_PULLED = 'notPulled'
}

